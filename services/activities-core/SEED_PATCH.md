# Seed Patch for Activities Core

- Add ~40 pangrams JSON file (already referenced); ensure loader runs at boot.
- Add `trivia_questions.json` and loader to insert if the table is empty.
- See backend scripts for details on loading logic.
