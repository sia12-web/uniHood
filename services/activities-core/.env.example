DATABASE_URL="postgresql://postgres:postgres@localhost:5432/activities_core"
REDIS_URL="redis://localhost:6379/0"
PORT=4005

# Strong secret required outside development.
# Clients send: Authorization: Bearer ${API_BEARER_TOKEN}:<userId>[:flags]
API_BEARER_TOKEN="replace-with-strong-secret"

# In development only, you may allow insecure bearer tokens (no secret prefix)
# ALLOW_INSECURE_BEARER=1

# Comma-separated list of allowed origins for CORS (e.g., Next dev server)
# Example: CORS_ORIGIN=http://localhost:3001
# CORS_ORIGIN=
