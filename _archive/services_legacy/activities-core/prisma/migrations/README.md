# Prisma Migrations

Run `pnpm --filter @divan/activities-core prisma:migrate` to generate the initial migration once the schema is finalized.

> TODO: Add initial migration for Activity/Session/Participant/Round/ScoreEvent models.
